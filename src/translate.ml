module Api = struct
  module PiqiToProtoc = struct
    let reference_person = assert false
    let search_params = assert false
    let index = assert false
    let list_reference_person = assert false
    let close_persons_params = assert false
    let person_start = assert false
    let error = assert false
    let list_persons = assert false
    let list_full_persons = assert false
  end
  module ProtocToPiqi = struct
    let reference_person = assert false
    let search_params = assert false
    let index = assert false
    let list_reference_person = assert false
    let close_persons_params = assert false
    let person_start = assert false
    let events_query_params = assert false
  end
end
module Api_saisie_write = struct
  module PiqiToProtoc = struct
    let auto_complete = assert false
    let person_search_list = assert false
    let person_search_list_params = assert false
    let index_person = assert false
    let person = assert false
    let index_person_and_family = assert false
  end
  module ProtocToPiqi = struct
    let auto_complete = assert false
    let person_search_list = assert false
    let person_search_list_params = assert false
    let index_person = assert false
    let person = assert false
    let index_person_and_family = assert false
  end
end
